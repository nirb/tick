{
  "name": "tick",
  "version": "1.0.0",
  "private": true,
  "description": "Tick - Family Task & Chore Progressive Web App (Cloudflare Stack)",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:server:remote": "npm run dev:remote --prefix server",
    "build": "npm run build --prefix client",
    "deploy:server": "npm run deploy --prefix server",
    "typecheck": "npm run typecheck --prefix server && npm run build --prefix client"
  }
}
